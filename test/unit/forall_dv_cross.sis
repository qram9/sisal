% for a cross b in A, B — cartesian product
function Main( A: array[integer]; B: array[integer] returns array_dv[integer] )
  for a in A cross b in B
  returns array_dv of a * b
  end for
end function
