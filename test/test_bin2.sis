define Main 

type IntList = union[Empty: null;Full: record[Mote: integer; Next:IntList]]

function Main(level: integer returns integer)
let x,y : INTEGER := level-1,level+1 in
let z : INTEGER := level-4 in
tempFun(x*y+2*3*z,x*z+2*3*y)
end let
end let
end function
